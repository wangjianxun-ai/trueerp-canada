# TrueERP

面向跨境电商的轻量 ERP 原型，包含产品、采购入库、批次库存/保质期、订单与发货管理。

## 启动

```bash
npm start
```

访问 `http://localhost:3000`。当前数据保存在浏览器本地存储中；产品主数据来自 `货品（导出1）.xlsx` 的 490 条记录。

## 天猫直购接口

当前为模拟同步。正式接入时建议由服务端实现订单增量拉取、签名验签、幂等去重、物流回传和失败重试，浏览器端不保存平台密钥。
